pipeline {
    agent {label'agentlinux'}

    stages {
        stage('Check versions') {
            steps {
                sh 'node --version'
                sh 'npm --version'
            }
        }

        
    }
}
